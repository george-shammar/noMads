<div class = "horizontal">
    <ul>
        <% @users.each do | user | %>
            <li>
                <%= link_to "#{user.username}", user_path(user) %>
            </li>

            <hr>
        <% end %>
    </ul>
</div>

